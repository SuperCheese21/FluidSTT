const fs = require('fs');
const Convert = require('./convert.js');

var request = function(file, speechToText, params) {
    console.log('To be implemented');
}

module.exports = {
    request: request
}
